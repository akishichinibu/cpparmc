PROJECT(cpparmc LANGUAGES CXX)
CMAKE_MINIMUM_REQUIRED(VERSION 3.13 FATAL_ERROR)

FILE(GLOB_RECURSE CPPARMC_SRC "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp")
MESSAGE("All source files: ${CPPARMC_SRC}")

ADD_LIBRARY(${PROJECT_NAME} STATIC ${CPPARMC_SRC})
